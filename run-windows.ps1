# Run UnisonX on Windows from WSL
# This script should be run from Windows PowerShell, not WSL

# Get the Windows path
$projectPath = "C:\unison"

# Change to project directory
cd $projectPath

# Start the application
npm run dev